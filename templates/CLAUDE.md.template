<!--
  Root CLAUDE.md. Keep it under 150 lines - past that it stops being read carefully.

  Write it BEFORE the code, so the rules constrain the work instead of describing it.

  Include only what cannot be inferred by reading the code. The test for every line:
  would removing it make someone pick the wrong file, command, or limit? If not, cut it.
  Never include tutorials, changelogs, generic language rules, or notes that will rot.

  Replace every {{PLACEHOLDER}} and delete what does not apply.
-->

# {{PROJECT_NAME}}

{{ONE_SENTENCE_WHAT_THIS_IS}}

## Stack

- {{STACK_AND_VERSION}}
- Package manager: {{PACKAGE_MANAGER}}

## Commands

Run these exactly; do not substitute equivalents.

| Purpose | Command |
|---|---|
| Install | `{{INSTALL_COMMAND}}` |
| Run locally | `{{RUN_COMMAND}}` |
| Full test suite | `{{TEST_COMMAND}}` |
| Lint / typecheck | `{{LINT_COMMAND}}` |

## Layout

{{WHERE_THINGS_LIVE_IN_3_TO_6_LINES}}

Structure: {{FOLDER_STRATEGY}}, recommended by {{WHO_RECOMMENDS_IT}}.

## Architectural rules

<!-- From the stack's `architecture.rules`. Every line checkable by reading the code:
     "the domain imports no SDK" is a rule, "keep it clean" is not. -->

- {{ARCHITECTURE_RULE_1}}
- {{ARCHITECTURE_RULE_2}}
- Do not add {{OPTIONAL_LAYER}} until {{THE_OBSERVED_REASON_THAT_WOULD_JUSTIFY_IT}}.

## Business rules

**They are not in this file.** They belong to the whole group, not to this repository:

    Business docs: {{PATH_OR_URL_TO_THE_GROUP_DOCS_REPO}}

<!-- Keep this line accurate even when the repository moves. It is the single instruction
     that survives without the skill installed, and every rule below depends on it. -->

Read it before changing behavior. When a change adds, removes or alters a business rule,
**update that document in the same task and commit it there**, naming the change that
caused it. If the repository is not on disk, say the entry is unwritten — never report it
as done.

Do not restate rules here. A bound kept in two places rots in one of them.

## Quality gate

- **Behavior changed** — a business rule, a contract, what the user sees — **then a test is
  written or updated**, covering the exact boundary from both sides, and the same number is
  written into the group's business-rules document.
- **Run `{{TEST_COMMAND}}` at the end of every task and report the real output.** Never
  claim done without it.
- **If it has a screen, launch it and look.** A green suite proves what you thought to
  assert. If you cannot launch it, render the changed screens at 320 and 360 logical pixels
  instead, and say the visual result is unverified.
- No coverage targets.

## Known landmines

<!-- Platform and library traps already paid for. Append the moment one is found, during
     the task. This is the only section that cannot be re-derived from the code. -->

- {{LANDMINE}} — {{WHAT_IT_BREAKS}} — {{WHAT_TO_DO_INSTEAD}}

## Never

- {{NEVER_1}}
- Delete a file because it looks unused. Removal needs written evidence of non-use and a
  passing suite.
