{
  "stack": "flutter",
  "display_name": "Flutter",
  "category": "mobile",
  "detection": {
    "files": [
      "pubspec.yaml"
    ],
    "markers": [
      "sdk: flutter",
      "flutter:"
    ]
  },
  "last_verified": "2026-08-27",
  "official_skills": [
    {
      "id": "flutter/agent-plugins",
      "owner": "Flutter team",
      "owner_kind": "framework-team",
      "install": "npx skills add flutter/agent-plugins --skill '*' --agent universal --yes",
      "covers": [
        "responsive-layouts",
        "declarative-routing",
        "json-serialization"
      ],
      "does_not_cover": [
        "testing-strategy",
        "app-architecture",
        "state-management-choice",
        "dead-code-hygiene"
      ],
      "source_url": "https://docs.flutter.dev/ai/agent-skills"
    },
    {
      "id": "dart-lang/skills",
      "owner": "Dart team",
      "owner_kind": "framework-team",
      "install": "npx skills add dart-lang/skills --skill '*' --agent universal --yes",
      "covers": [
        "unit-test-generation",
        "package-dependency-resolution",
        "static-analysis-fixes"
      ],
      "does_not_cover": [
        "widget-tests",
        "golden-tests",
        "integration-tests",
        "test-strategy"
      ],
      "source_url": "https://docs.flutter.dev/ai/agent-skills",
      "notes": "Generates unit tests. It does not decide which layer a given behavior belongs in - that judgment stays with the skill."
    },
    {
      "id": "LambdaTest/agent-skills#flutter-testing-skill",
      "owner": "TestMu AI (formerly LambdaTest)",
      "owner_kind": "vendor",
      "install": "npx agentskillsforall add https://github.com/LambdaTest/agent-skills.git --skill flutter-testing-skill",
      "covers": [
        "flutter-test-authoring"
      ],
      "does_not_cover": [
        "test-strategy",
        "golden-test-ci-pinning"
      ],
      "source_url": "https://github.com/LambdaTest/agent-skills",
      "notes": "Vendor skill, not a framework-team skill. It uses a different CLI (agentskillsforall) than the skills CLI. Recommend it as an option, not as the default."
    }
  ],
  "gap_map": [
    {
      "gap": "testing-strategy",
      "why_it_matters": "No Flutter framework-team skill decides which behavior belongs in a unit, widget, golden or integration test. Teams default to whatever is easiest to write, which is usually the slowest layer.",
      "filled_by": "LambdaTest/agent-skills#flutter-testing-skill",
      "fallback": "Apply rule 2 of SKILL.md directly: business rules and presentation rules, at the exact boundary, plus a real run of the suite."
    },
    {
      "gap": "app-architecture",
      "why_it_matters": "Layout and routing skills operate inside a widget. Nothing tells the agent when a feature folder has outgrown itself or when a widget should become a package.",
      "filled_by": null,
      "fallback": "Apply rule 1 of SKILL.md and measure against this file's growth_thresholds."
    },
    {
      "gap": "golden-test-environment",
      "why_it_matters": "Golden tests fail across platforms because of font and pixel rendering differences. A test suite that only passes on one machine erodes trust in the whole suite.",
      "filled_by": null,
      "fallback": "Require goldens to run on a pinned CI image and treats local golden failures as environment noise until proven otherwise."
    }
  ],
  "testing": {
    "layers": [
      {
        "name": "unit",
        "belongs_here": "a single function, method or class with its dependencies mocked. View model logic, progress and pricing rules, parsing, error paths.",
        "does_not_belong_here": "anything that needs a widget tree to observe",
        "cost": "Flutter team: confidence low, maintenance low, fast. Cheapest place to enumerate branches.",
        "signal_you_picked_wrong": "the test calls pumpWidget to check a value a function could return"
      },
      {
        "name": "widget",
        "belongs_here": "one widget: that its UI looks and interacts as expected. Taps, form validation surfacing, conditional rendering.",
        "does_not_belong_here": "business rules reachable from a view model, and whole-flow behaviour",
        "cost": "Flutter team: confidence higher, maintenance higher, still fast.",
        "signal_you_picked_wrong": "the test enumerates business branches by tapping through the UI"
      },
      {
        "name": "golden",
        "belongs_here": "visual regression on stable, deliberate UI - a design system component, a screen whose layout must not drift",
        "does_not_belong_here": "behaviour of any kind; a golden asserts pixels, not logic",
        "cost": "high: every legitimate design change invalidates the file, and it fails for environment reasons",
        "signal_you_picked_wrong": "the golden churns on changes nobody considers visual"
      },
      {
        "name": "integration",
        "belongs_here": "a complete app or a large part of it: that widgets and services work together. The handful of flows whose breakage is an incident.",
        "does_not_belong_here": "coverage of branches - that is what unit tests are for",
        "cost": "Flutter team: confidence highest, maintenance highest, slow.",
        "signal_you_picked_wrong": "the suite takes long enough that people stop running it locally"
      }
    ],
    "proportions": "Flutter team: a well-tested app has many unit and widget tests, tracked by code coverage, plus enough integration tests to cover all the important use cases. The shape is the point - heavy at the fast base, sparing at the slow tip.",
    "commands": {
      "full_suite": "flutter test",
      "unit_and_widget": "flutter test",
      "golden_update": "flutter test --update-goldens",
      "integration": "flutter test integration_test",
      "coverage": "flutter test --coverage",
      "analyze": "flutter analyze"
    },
    "rules": [
      "A test that needs pumpWidget is testing the View. If the same assertion can be made against the view model, move it down a layer.",
      "Goldens are generated in CI, never on a developer machine. A golden produced locally and validated in CI gives an indeterminate failure nobody can act on.",
      "Register a bundled test font in the test bootstrap. A golden that depends on system fonts fails across macOS, Linux and Windows for reasons unrelated to the change.",
      "Never call pumpAndSettle on a tree with an indefinite animation - a loading spinner never settles and the test times out. Pump a fixed number of frames, or freeze the clock.",
      "Never regenerate goldens to make a failure disappear without reading the diff. That is how a real regression gets committed as the new expected output.",
      "Integration tests cover flows whose breakage is an incident. Adding one to raise coverage is the wrong reason."
    ],
    "traps": [
      "pumpAndSettle times out on any indefinite animation - the most common false failure in the stack.",
      "Goldens generated on one OS and validated on another differ through font rendering alone, so the suite goes red on a change that altered nothing visual.",
      "Anything with a timestamp, a randomised layout, a mid-frame animation or a network image is a bad golden candidate: it fails on its own schedule.",
      "Mocking a repository in a widget test hides the wiring the widget test existed to check - if everything below is mocked, the test asserts the mock.",
      "A green suite with generated code out of date: build_runner output is committed, so stale *.g.dart passes tests and fails at runtime.",
      "simctl io ... screenshot right after simctl boot fails with \"Device does not have a 'default' display port\". It is a race, not a limitation: the display exists a few seconds after boot returns. Wait and retry before concluding you cannot look. Several simulators run at once, and installing your app on one that already holds another app disturbs nothing - a busy simulator is not a reason to skip verification."
    ],
    "what_not_to_test": [
      "That setState rebuilt, or that a StatelessWidget is stateless - that tests the framework, not the product.",
      "Generated code (*.g.dart, *.freezed.dart, *.mocks.dart). It is the generator's contract, not yours.",
      "Getters, constructors and pass-through methods with no branching.",
      "Exact widget tree shape. It changes on every refactor and asserts implementation, not behaviour."
    ],
    "notes": "Golden tests are visual regression, not behavior. They need a pinned CI environment to avoid cross-platform pixel differences. Never regenerate goldens to make a failure disappear without looking at the diff."
  },
  "growth_thresholds": {
    "extract_component_library": ">=3 occurrences of the same widget across >=2 feature folders",
    "split_feature": "a feature folder exceeds 15 files",
    "extract_package": ">=2 features import the same internal module",
    "split_widget": "a build() method exceeds 100 lines, because the widget is doing layout and logic at once"
  },
  "architecture": {
    "recommended_by": "framework-team",
    "source_url": "https://docs.flutter.dev/app-architecture/guide",
    "pattern": "layered MVVM",
    "folder_strategy": "feature-first",
    "layers": [
      {
        "name": "UI - View",
        "holds": "widget classes that render",
        "must_not": "hold business logic; only conditional rendering, animation, layout and simple routing"
      },
      {
        "name": "UI - ViewModel",
        "holds": "most of the application logic: transforms repository data, holds UI state, exposes commands",
        "must_not": "know about widgets it does not own - one view maps to exactly one view model"
      },
      {
        "name": "Data - Repository",
        "holds": "the source of truth for model data: caching, retries, error handling, transforming raw data into domain models",
        "must_not": "know about another repository"
      },
      {
        "name": "Data - Service",
        "holds": "wrappers over external sources - REST endpoints, platform APIs, local files - returning Future or Stream",
        "must_not": "contain business logic"
      },
      {
        "name": "Domain - Use case",
        "optional": true,
        "holds": "logic that merges several repositories or is duplicated across view models",
        "must_not": "exist before a second view model needs it"
      }
    ],
    "variants": [
      {
        "name": "Two layers (UI + Data), no domain layer",
        "use_when": "the default. View models talk to repositories directly.",
        "cost": "logic shared by several view models gets duplicated before you notice it"
      },
      {
        "name": "Three layers (UI + Domain + Data)",
        "use_when": "a second view model needs the same composed logic, or one view model has to merge several repositories",
        "cost": "one more hop for every read; premature use-cases are the classic over-abstraction signal"
      }
    ],
    "rules": [
      "A view holds no business logic. Conditional rendering, animation, layout and simple routing only.",
      "One view maps to exactly one view model. A view model serving two views is a signal the feature is really two.",
      "A repository never imports another repository. Composition across repositories belongs in a use case.",
      "A service returns raw data. Business rules live above it.",
      "Do not add the domain layer until a second view model needs the same logic."
    ],
    "component_library": "Extract to a shared component library at >=3 occurrences of the same widget across >=2 features - matching growth_thresholds.extract_component_library. Atomic Design is a way to organise that library once it exists; it is not a reason to create one.",
    "notes": "The Flutter team defines a feature as one paired view + view model, which is why the folder strategy is feature-first rather than layer-first."
  },
  "key_decisions": [
    {
      "decision": "State management",
      "why_it_is_forced": "Flutter ships setState and nothing above it. Every app past one screen picks something, and the framework takes no position.",
      "options": [
        "setState only",
        "Provider",
        "Riverpod",
        "Bloc/Cubit",
        "signals"
      ],
      "reasonable_default": "The one the team already knows. There is no winner; there is a cost to using two.",
      "cost_of_getting_it_wrong": "It reaches every view model and every test setup. Changing it later is a rewrite of the UI layer, not a refactor."
    },
    {
      "decision": "Navigation",
      "why_it_is_forced": "Navigator 1.0 and the Router API coexist, and deep links, web URLs and nested shells behave differently under each.",
      "options": [
        "Navigator 1.0",
        "go_router",
        "Router API directly",
        "auto_route"
      ],
      "reasonable_default": "go_router when deep links or web are in scope; Navigator 1.0 when neither is.",
      "cost_of_getting_it_wrong": "Retrofitting deep links onto Navigator 1.0 touches every route in the app."
    },
    {
      "decision": "Domain layer: present or absent",
      "why_it_is_forced": "The Flutter team marks the use-case layer optional, so it is a real choice, not an omission.",
      "options": [
        "two layers (UI + Data)",
        "three layers (UI + Domain + Data)"
      ],
      "reasonable_default": "Two layers. Add the domain layer when a second view model needs the same composed logic.",
      "cost_of_getting_it_wrong": "Adding it early is the over-abstraction signal; adding it late means moving logic out of several view models at once."
    },
    {
      "decision": "Immutability and code generation",
      "why_it_is_forced": "Dart has no built-in data classes, so equality, copyWith and JSON are either generated or hand-written.",
      "options": [
        "freezed + json_serializable",
        "hand-written classes",
        "dart_mappable"
      ],
      "reasonable_default": "Generation once the model count passes a handful; hand-written below that.",
      "cost_of_getting_it_wrong": "Generation adds a build step to every developer and every CI run, and generated files must never be edited or deleted by hand."
    },
    {
      "decision": "Persistence",
      "why_it_is_forced": "Flutter bundles no storage layer.",
      "options": [
        "shared_preferences",
        "sqflite/drift",
        "Isar/Hive",
        "remote only"
      ],
      "reasonable_default": "shared_preferences for flags and small records; a real database as soon as anything is queried.",
      "cost_of_getting_it_wrong": "Migrating stored data in released apps needs a migration path you did not plan for."
    }
  ],
  "operability": {
    "failure_boundaries": [
      "HTTP clients and any Service class",
      "platform channels",
      "local database and shared_preferences",
      "push and deep-link entry points"
    ],
    "observability": "There is no console on a user's device: crash and error reporting must be wired to a service or failures are invisible to you.",
    "authorization": "Enforced on the server. A client-side check is a UX affordance, never a control - the app binary is in the user's hands.",
    "done_checks": [
      "flutter analyze",
      "flutter test",
      "flutter build (for each shipped platform)"
    ],
    "rules": [
      "Every asynchronous operation a user can see declares four states: loading, empty, error and success. Empty is not success with no rows, and success on a mutation is a confirmation, not a silent return to normal.",
      "Loading affordance matches what you know: a progress bar with a percentage only for determinate work; a skeleton or spinner otherwise. Never a percentage you cannot compute.",
      "Every Service states a timeout and a retry count. A Future with no timeout hangs the UI with no error to show.",
      "Every feature that writes data states its offline behaviour, even if the decision is to fail loudly.",
      "Stored data has a migration path. You cannot ask users to reinstall.",
      "A denied permission is a supported path, not a crash.",
      "Assume several app versions are live at once - a server change that only works with the newest client breaks the rest."
    ]
  },
  "dead_code_risks": [
    "Generated code (*.g.dart, *.freezed.dart, *.mocks.dart) - never edit or delete by hand, regenerate with build_runner",
    "Widgets referenced only by golden tests",
    "Route names referenced as strings in a router table",
    "Classes registered by name in a service locator (get_it and similar)",
    "Platform channel handlers referenced only from Kotlin/Swift sources"
  ]
}
