{
  "stack": "dotnet",
  "display_name": ".NET / ASP.NET Core",
  "category": "backend",
  "detection": {
    "files": [
      "*.csproj",
      "*.sln",
      "global.json",
      "Directory.Build.props"
    ],
    "markers": [
      "Microsoft.NET.Sdk.Web",
      "<TargetFramework>",
      "WebApplication.CreateBuilder"
    ]
  },
  "last_verified": "2026-08-29",
  "official_skills": [
    {
      "id": "dotnet/skills#dotnet-aspnetcore",
      "owner": ".NET team",
      "owner_kind": "framework-team",
      "install": "/plugin marketplace add dotnet/skills   then   /plugin install dotnet-aspnetcore@dotnet-agent-skills",
      "covers": [
        "middleware",
        "minimal-and-controller-endpoints",
        "real-time-communication",
        "api-patterns"
      ],
      "does_not_cover": [
        "test-strategy",
        "architecture-review",
        "dead-code-hygiene"
      ],
      "source_url": "https://github.com/dotnet/skills",
      "notes": "Distributed as Claude Code / Copilot CLI plugins through a marketplace, not through the skills CLI. Two commands, in this order."
    },
    {
      "id": "dotnet/skills#dotnet-test",
      "owner": ".NET team",
      "owner_kind": "framework-team",
      "install": "/plugin marketplace add dotnet/skills   then   /plugin install dotnet-test@dotnet-agent-skills",
      "covers": [
        "test-execution",
        "test-filtering",
        "platform-detection",
        "coverage",
        "testability-review",
        "mstest-workflows"
      ],
      "does_not_cover": [
        "test-strategy"
      ],
      "source_url": "https://github.com/dotnet/skills",
      "notes": "This is the closest any stack in the registry comes to a framework-team testing skill: it covers running and improving tests, including testability. It still does not decide which behavior deserves a test."
    },
    {
      "id": "dotnet/skills#dotnet-data",
      "owner": ".NET team",
      "owner_kind": "framework-team",
      "install": "/plugin marketplace add dotnet/skills   then   /plugin install dotnet-data@dotnet-agent-skills",
      "covers": [
        "data-access",
        "entity-framework"
      ],
      "does_not_cover": [
        "migration-deploy-strategy"
      ],
      "source_url": "https://github.com/dotnet/skills"
    },
    {
      "id": "LambdaTest/agent-skills#xunit-skill",
      "owner": "TestMu AI (formerly LambdaTest)",
      "owner_kind": "vendor",
      "install": "npx agentskillsforall add https://github.com/LambdaTest/agent-skills.git --skill xunit-skill",
      "covers": [
        "xunit-authoring",
        "fact-and-theory",
        "fluentassertions"
      ],
      "does_not_cover": [
        "test-strategy"
      ],
      "source_url": "https://github.com/LambdaTest/agent-skills",
      "notes": "nunit-skill and mstest-skill exist in the same repository for projects on those runners. Prefer dotnet/skills#dotnet-test first - it is the framework-team option."
    }
  ],
  "gap_map": [
    {
      "gap": "test-strategy",
      "why_it_matters": "dotnet-test covers execution and testability but not selection. The judgment about which behavior earns a test stays uncovered.",
      "filled_by": null,
      "fallback": "Apply rule 2 of SKILL.md directly: business rules and presentation rules, at the exact boundary, plus a real run of the suite."
    },
    {
      "gap": "solution-and-project-boundaries",
      "why_it_matters": ".NET solutions accumulate projects. Nothing decides when a folder should become a project, or when a shared project has turned into a grab bag that every other project references.",
      "filled_by": null,
      "fallback": "Apply rule 1 of SKILL.md and measure against this file's growth_thresholds."
    }
  ],
  "testing": {
    "layers": [
      {
        "name": "unit",
        "belongs_here": "individual components or methods - and, per Microsoft's own guidance, only code within the developer's control: unit tests do not test infrastructure concerns such as databases, file systems or network resources",
        "does_not_belong_here": "anything touching infrastructure",
        "cost": "cheapest and fastest",
        "signal_you_picked_wrong": "the test needs a database or a WebApplicationFactory to reach one assertion"
      },
      {
        "name": "integration",
        "belongs_here": "two or more components working together - and, again per Microsoft, these often do include infrastructure concerns",
        "does_not_belong_here": "exhaustive branch coverage",
        "cost": "slower; needs real or containerised dependencies"
      },
      {
        "name": "functional (WebApplicationFactory)",
        "belongs_here": "the real pipeline: middleware, model binding, filters, authorization and DI wiring",
        "does_not_belong_here": "business branch enumeration",
        "cost": "moderate, and it catches the mistakes unit tests structurally cannot see"
      },
      {
        "name": "load",
        "belongs_here": "whether the system handles a specified load - Microsoft lists this as a first-class test type",
        "does_not_belong_here": "correctness",
        "cost": "needs an environment resembling production to mean anything"
      }
    ],
    "proportions": "No proportions stated by Microsoft. Their definition sets the boundary instead: unit tests do not touch infrastructure, integration tests often do. That line decides where a test belongs more reliably than any ratio.",
    "commands": {
      "full_suite": "dotnet test",
      "coverage": "dotnet test --collect:\"XPlat Code Coverage\"",
      "build": "dotnet build",
      "format": "dotnet format --verify-no-changes"
    },
    "rules": [
      "A unit test that reaches a database, the file system or the network is an integration test that has been mislabelled - Microsoft's own definition draws that line.",
      "Use WebApplicationFactory for integration tests over the real pipeline: it catches the middleware, authorization and DI mistakes unit tests cannot.",
      "dotnet test runs every test project in the solution. Run it, not a filtered subset, before reporting a task complete.",
      "Pick one test framework per solution. MSTest, NUnit, TUnit and xUnit are all supported; mixing them doubles the conventions a contributor must know."
    ],
    "traps": [
      "Async tests returning void instead of Task: exceptions are swallowed and the test passes regardless of what happened.",
      "An in-memory EF Core provider accepts queries and constraint violations that a real database rejects, so the suite is green and the deployment is not.",
      "A shared WebApplicationFactory carries state between tests, so a test passes alone and fails in the suite - or the reverse.",
      "Static state and singletons persist across tests in the same assembly; test order then changes the result.",
      "Mocking DbContext produces a test that asserts the mock's behaviour rather than any query the database would run."
    ],
    "what_not_to_test": [
      "Auto-properties, constructors and pass-through methods with no branching.",
      "Framework behaviour: that model binding bound, that the DI container resolved.",
      "EF Core migration classes themselves; assert on the resulting schema.",
      "Generated code and scaffolded output."
    ],
    "notes": "WebApplicationFactory gives real integration tests over the actual pipeline, which catches the middleware and DI mistakes that unit tests structurally cannot see."
  },
  "growth_thresholds": {
    "split_controller": "a controller exceeds 10 endpoints",
    "extract_service_layer": "an endpoint or action method exceeds 30 lines",
    "split_service": "a class exceeds 5 constructor-injected dependencies",
    "extract_project": ">=2 projects reference the same internal folder by relative path"
  },
  "architecture": {
    "recommended_by": "framework-team",
    "source_url": "https://learn.microsoft.com/en-us/dotnet/architecture/modern-web-apps-azure/common-web-application-architectures",
    "pattern": "layered solution with projects as the boundary",
    "folder_strategy": "feature-first",
    "layers": [
      {
        "name": "Web / API project",
        "holds": "endpoints, middleware, model binding, the composition root where DI is configured",
        "must_not": "business rules - an action method past ~30 lines has absorbed the service"
      },
      {
        "name": "Application / service layer",
        "holds": "business rules and use cases, callable without HttpContext",
        "must_not": "knowledge of the transport or of EF Core specifics"
      },
      {
        "name": "Domain",
        "holds": "entities and invariants",
        "must_not": "persistence concerns",
        "optional": true
      },
      {
        "name": "Infrastructure",
        "holds": "EF Core, external services, file and network access",
        "must_not": "business policy - it implements interfaces the layers above define"
      }
    ],
    "variants": [
      {
        "name": "Single project",
        "use_when": "small API, one team, short-lived",
        "cost": "nothing enforces the layer boundary but review; it erodes quietly"
      },
      {
        "name": "Layered projects in one solution",
        "use_when": "the common shape: Web, Application, Infrastructure, and Domain if the rules justify it",
        "cost": "project references must be maintained, and a wrong one is how Infrastructure ends up referenced by Domain"
      },
      {
        "name": "Vertical slices",
        "use_when": "features are independent and each owns its request, handler and data access end to end",
        "cost": "cross-cutting concerns get duplicated per slice unless deliberately shared"
      }
    ],
    "rules": [
      "Project references are the architecture. Web references Application, Application references Domain, Infrastructure implements interfaces defined above it - and Domain references nothing.",
      "An action or endpoint translates HTTP and delegates. Business rules go where they can be tested without a WebApplicationFactory.",
      "The composition root is the Web project. Nothing below it constructs its own dependencies.",
      "Every new endpoint states its authorization decision explicitly, including [AllowAnonymous] on purpose.",
      "Never let EF Core types leak past Infrastructure: an IQueryable returned upward makes the database schema part of every caller's contract."
    ],
    "component_library": "Not applicable. Promote shared code to its own project at >=2 consuming projects; below that, a folder is enough.",
    "notes": "Microsoft publishes several reference architectures rather than one mandate. What is not negotiable in any of them is the direction of project references, which is why the rules focus there."
  },
  "key_decisions": [
    {
      "decision": "Minimal APIs or controllers",
      "why_it_is_forced": "Both are first-class in ASP.NET Core and they organise, test and version differently.",
      "options": [
        "minimal APIs",
        "controllers",
        "both, split by area"
      ],
      "reasonable_default": "Minimal APIs for small surfaces; controllers when filters, conventions and model binding attributes are doing real work.",
      "cost_of_getting_it_wrong": "Mixing them without a rule means two places to enforce authorization and two conventions for every contributor."
    },
    {
      "decision": "Test platform and framework",
      "why_it_is_forced": "Microsoft documents two platforms - VSTest and Microsoft.Testing.Platform - and four frameworks. Nothing picks for you.",
      "options": [
        "xUnit",
        "NUnit",
        "MSTest",
        "TUnit"
      ],
      "reasonable_default": "One per solution, chosen once. All four are supported; consistency is what matters.",
      "cost_of_getting_it_wrong": "A solution with two frameworks needs two sets of conventions, two runners and two CI configurations."
    },
    {
      "decision": "Data access",
      "why_it_is_forced": "EF Core, Dapper and ADO.NET are all supported and shape the code above them differently.",
      "options": [
        "EF Core",
        "Dapper",
        "raw ADO.NET",
        "EF Core plus Dapper for hot paths"
      ],
      "reasonable_default": "EF Core unless the queries are the product.",
      "cost_of_getting_it_wrong": "EF Core's change tracking and lazy loading leak upward, so replacing it later is not a swap."
    },
    {
      "decision": "Project boundaries in the solution",
      "why_it_is_forced": "A solution can be one project or ten, and project references are what actually enforce the architecture.",
      "options": [
        "single project",
        "layered projects",
        "vertical slices"
      ],
      "reasonable_default": "Single project until a boundary is real; then layered, with references pointing one way only.",
      "cost_of_getting_it_wrong": "A reference added in the wrong direction is trivial to add and expensive to remove once code depends on it."
    },
    {
      "decision": "Hosting and deployment target",
      "why_it_is_forced": "Available features and startup behaviour differ between a long-lived host and a serverless one.",
      "options": [
        "Azure App Service",
        "container",
        "AWS Lambda",
        "self-hosted Kestrel"
      ],
      "reasonable_default": "Decide before building.",
      "cost_of_getting_it_wrong": "Background services and in-process caches that work under a long-lived host silently do nothing under serverless."
    }
  ],
  "operability": {
    "failure_boundaries": [
      "HttpClient calls",
      "EF Core and database access",
      "message brokers",
      "background services and hosted services",
      "third-party SDKs"
    ],
    "observability": "ILogger with structured logging, and OpenTelemetry for traces and metrics. Exception handling middleware is where an unhandled exception becomes a recorded, shaped problem response instead of a bare 500.",
    "authorization": "Authorization policies. Set a default policy so endpoints must opt out with [AllowAnonymous] - then an unmarked endpoint is protected rather than public.",
    "done_checks": [
      "dotnet build",
      "dotnet test",
      "dotnet format --verify-no-changes",
      "the app starts with the production configuration",
      "clean clone and build from scratch"
    ],
    "rules": [
      "Register HttpClient through IHttpClientFactory with an explicit timeout. A raw HttpClient with the default timeout and no policy exhausts sockets and waits far too long.",
      "Set a default authorization policy so a forgotten attribute fails closed instead of open.",
      "Exception handling middleware reports unhandled exceptions. Without it they become 500s with no record.",
      "Configuration is validated at startup with options validation, so a missing value fails the deploy rather than the first request.",
      "EF Core migrations are applied deliberately, never automatically at startup in production - an automatic migration on a rolling deploy runs from several instances at once."
    ]
  },
  "dead_code_risks": [
    "Types resolved from the DI container by interface rather than by direct construction",
    "Controllers and endpoints reachable only through routing",
    "Types instantiated by reflection, including serialization targets and source-generated code",
    "EF Core migration classes discovered by assembly scan",
    "Anything referenced only from appsettings.json",
    "public members of a class library consumed by another solution",
    "Code behind a feature flag that is currently off"
  ]
}
