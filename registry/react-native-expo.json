{
  "stack": "react-native-expo",
  "display_name": "React Native / Expo",
  "category": "mobile",
  "detection": {
    "files": [
      "app.json",
      "app.config.ts",
      "app.config.js",
      "package.json"
    ],
    "markers": [
      "\"expo\":",
      "\"react-native\":",
      "expo-router"
    ]
  },
  "last_verified": "2026-08-29",
  "official_skills": [
    {
      "id": "expo/skills",
      "owner": "Expo team",
      "owner_kind": "framework-team",
      "install": "npx skills@latest add expo/skills --skill '*'",
      "skills": [
        "expo-overview",
        "expo-project-structure",
        "expo-router",
        "expo-animation",
        "expo-native-ui",
        "expo-design-system",
        "expo-ui",
        "expo-data-fetching",
        "expo-tailwind-setup",
        "expo-dom",
        "expo-web-to-native",
        "expo-module",
        "expo-brownfield",
        "expo-dev-client",
        "expo-app-clip",
        "expo-upgrade",
        "eas-app-stores",
        "eas-hosting",
        "eas-workflows",
        "eas-observe",
        "eas-update-insights",
        "eas-simulator"
      ],
      "covers": [
        "project-structure",
        "routing",
        "native-ui",
        "design-system",
        "data-fetching",
        "styling-setup",
        "native-modules",
        "brownfield-integration",
        "dev-clients",
        "upgrades",
        "store-submission",
        "build-workflows",
        "observability"
      ],
      "does_not_cover": [
        "test-strategy",
        "performance-optimization-depth",
        "dead-code-hygiene"
      ],
      "source_url": "https://github.com/expo/skills",
      "notes": "Start with expo-overview. This is the largest framework-team collection of the eight stacks; installing all of it is reasonable. A Claude Code plugin route also exists: claude plugin install expo@claude-plugins-official"
    },
    {
      "id": "callstackincubator/agent-skills#react-native-best-practices",
      "owner": "Callstack",
      "owner_kind": "community",
      "install": "npx skills@latest add callstackincubator/agent-skills --skill react-native-best-practices",
      "covers": [
        "fps-profiling",
        "time-to-interactive",
        "bundle-size",
        "memory-leaks",
        "re-render-analysis",
        "animation-performance"
      ],
      "does_not_cover": [
        "test-strategy",
        "app-architecture"
      ],
      "source_url": "https://github.com/callstackincubator/agent-skills",
      "notes": "Callstack are long-time React Native core contributors, but this is not a framework-team repository. Recommend it when the observed defect is a performance defect, not by default."
    }
  ],
  "gap_map": [
    {
      "gap": "test-strategy",
      "why_it_matters": "Neither collection decides what deserves a test on a mobile client, where the slow layers (device E2E) are extremely tempting and extremely expensive.",
      "filled_by": "LambdaTest/agent-skills#detox-skill for authoring device tests",
      "fallback": "Apply rule 2 of SKILL.md directly: business rules and presentation rules, at the exact boundary, plus a real run of the suite.",
      "needs_verification": true
    },
    {
      "gap": "offline-and-state-sync",
      "why_it_matters": "Mobile apps lose connectivity. Data-fetching guidance assumes the happy path; nothing audits what happens to in-flight writes when the network drops.",
      "filled_by": null,
      "fallback": "Require an explicit offline decision for every feature that writes data, even if the decision is 'fail loudly'."
    }
  ],
  "testing": {
    "layers": [
      {
        "name": "unit",
        "belongs_here": "business rules, reducers, formatting, hooks with no native dependency",
        "does_not_belong_here": "anything touching a native module",
        "cost": "cheap and fast",
        "signal_you_picked_wrong": "the test needs a navigation container to reach one assertion"
      },
      {
        "name": "component",
        "belongs_here": "how a screen renders and reacts to interaction, with native modules faked",
        "does_not_belong_here": "navigation flows across several screens",
        "cost": "moderate",
        "signal_you_picked_wrong": "the assertions are about the component tree rather than what the user sees"
      },
      {
        "name": "integration",
        "belongs_here": "a flow across screens with the navigation container mounted",
        "does_not_belong_here": "branch coverage",
        "cost": "slower and setup-heavy"
      },
      {
        "name": "device-e2e",
        "belongs_here": "flows whose breakage is a production incident, on a real device or simulator",
        "does_not_belong_here": "anything else - this is the most expensive layer in the whole registry",
        "cost": "slowest, flakiest and hardware-dependent"
      }
    ],
    "proportions": "No proportions stated by the Expo team. The cost asymmetry sets the shape: device E2E is slow, flaky and hardware-dependent, so keep it to a handful of flows and push everything else down.",
    "commands": {
      "full_suite": "npm test",
      "typecheck": "npx tsc --noEmit",
      "lint": "npm run lint",
      "doctor": "npx expo-doctor"
    },
    "rules": [
      "Anything a native module touches must be faked at the boundary, or the test cannot run outside a device.",
      "Run npx expo-doctor before believing a green suite: a version mismatch between Expo SDK and a dependency fails at runtime, not in tests.",
      "Device E2E covers flows whose breakage is an incident. Adding one to raise coverage buys nothing and costs the most.",
      "Read package.json before assuming a runner."
    ],
    "traps": [
      "A suite that passes in JS while the native build is broken - unit and component tests never load the native side.",
      "Mocking the navigation container hides exactly the wiring that navigation tests existed to check.",
      "A test passing on the simulator and failing on device, or on one OS only: fonts, safe areas, permissions and keyboard behaviour all differ.",
      "Timers and animations: a test that waits on a real interval is flaky by construction; fake the clock.",
      "Expo Go behaving differently from a development build, so a green run under Expo Go says nothing about the shipped app."
    ],
    "what_not_to_test": [
      "That a screen renders without crashing, with no assertion about what it shows.",
      "Framework behaviour: that expo-router matched a file to a URL.",
      "Exact component tree shape.",
      "Generated native project files under ios/ and android/."
    ],
    "notes": "Device E2E is the most expensive layer in any stack in this registry: slow, flaky and hardware dependent. Keep it to the handful of flows whose breakage would be a production incident. Read package.json scripts before assuming a runner."
  },
  "growth_thresholds": {
    "extract_component_library": ">=3 occurrences of the same component across >=2 screens or features",
    "split_feature": "a feature folder exceeds 15 files",
    "extract_package": ">=2 features import the same internal module",
    "split_screen": "a screen component exceeds 200 lines, which on mobile usually means layout, navigation and data are entangled"
  },
  "architecture": {
    "recommended_by": "framework-team",
    "source_url": "https://docs.expo.dev/develop/project-structure/",
    "pattern": "file-based routing with expo-router",
    "folder_strategy": "feature-first",
    "layers": [
      {
        "name": "src/app/",
        "holds": "the route tree. The file structure here determines the app's navigation, and _layout.tsx configures each navigator.",
        "must_not": "anything not routable - a file dropped here becomes a screen"
      },
      {
        "name": "components/",
        "holds": "presentational components shared across routes",
        "must_not": "data fetching and navigation decisions"
      },
      {
        "name": "native module / config plugin",
        "holds": "code crossing into Kotlin or Swift, and build-time config",
        "must_not": "anything expressible in JS - every native module is a rebuild for every developer"
      }
    ],
    "variants": [
      {
        "name": "Routes in src/app, everything else beside it",
        "use_when": "the default; the docs describe src/app as where navigation is defined",
        "cost": "shared folders grow flat and stop meaning anything past a few features"
      },
      {
        "name": "Feature folders with routes colocated as thin re-exports",
        "use_when": "features are genuinely independent and each owns its screens, hooks and data",
        "cost": "one more indirection between the URL and the screen, and newcomers must learn it"
      }
    ],
    "rules": [
      "The file structure of src/app IS the navigation. Never put a non-routable file there - it becomes a screen.",
      "_layout.tsx is where a navigator is configured. Navigation structure changes happen there, not by conditional rendering inside a screen.",
      "A screen file wires data to presentation. Business rules live outside src/app so they can be tested without a navigation container.",
      "Every feature that writes data states its offline behaviour explicitly, even if the decision is to fail loudly. Mobile clients lose connectivity as a normal condition, not an error.",
      "Adding a native module or config plugin means every developer and every CI run needs a new build. Decide it deliberately."
    ],
    "component_library": "Promote at >=3 occurrences across >=2 screens. On mobile, prefer composition over configuration flags: a component with six booleans is cheaper to split than to keep.",
    "notes": "The Expo docs describe src/app and file-based routing, and do not state where non-route code belongs. The component and feature placement above is this catalog's own doctrine - present it as such, not as Expo's recommendation."
  },
  "key_decisions": [
    {
      "decision": "Managed workflow or native projects committed",
      "why_it_is_forced": "Expo supports both, and committing ios/ and android/ changes how upgrades and config plugins work forever.",
      "options": [
        "managed (CNG, no native dirs committed)",
        "native projects committed"
      ],
      "reasonable_default": "Managed, until a native dependency genuinely requires committed projects.",
      "cost_of_getting_it_wrong": "Once native directories are committed, every Expo upgrade becomes a manual merge instead of a regeneration."
    },
    {
      "decision": "Navigation structure",
      "why_it_is_forced": "expo-router derives navigation from the filesystem, so the folder layout is the navigation contract.",
      "options": [
        "tabs",
        "stack",
        "drawer",
        "nested combinations"
      ],
      "reasonable_default": "Start with the simplest that fits; nesting is easy to add and expensive to remove.",
      "cost_of_getting_it_wrong": "Restructuring navigation later changes every deep link and every URL the app has published."
    },
    {
      "decision": "Offline and data sync",
      "why_it_is_forced": "Mobile clients lose connectivity as a normal condition and no data layer is bundled.",
      "options": [
        "online only, fail loudly",
        "cache and retry",
        "local-first with sync"
      ],
      "reasonable_default": "Online only with explicit failure, unless the product promises offline use.",
      "cost_of_getting_it_wrong": "Retrofitting local-first means rewriting every write path and inventing conflict resolution after the fact."
    },
    {
      "decision": "Build and release pipeline",
      "why_it_is_forced": "Store submission, OTA updates and build infrastructure are decisions, not defaults.",
      "options": [
        "EAS Build and Submit",
        "self-hosted CI",
        "local builds"
      ],
      "reasonable_default": "EAS unless there is a reason not to.",
      "cost_of_getting_it_wrong": "Discovering the pipeline at release time is how a launch date slips by weeks."
    },
    {
      "decision": "State management",
      "why_it_is_forced": "React Native ships no state solution above component state.",
      "options": [
        "React state and context",
        "Zustand",
        "Redux Toolkit",
        "TanStack Query for server state"
      ],
      "reasonable_default": "Component state plus a query library for server data; a global store only when several unrelated screens share writable state.",
      "cost_of_getting_it_wrong": "It reaches every screen and every test setup."
    }
  ],
  "operability": {
    "failure_boundaries": [
      "network requests",
      "local storage reads and writes",
      "native module calls",
      "push notification and deep-link entry points",
      "over-the-air update checks"
    ],
    "observability": "There is no console on a user's device. Crash and error reporting must be wired to a service - Expo publishes EAS Observe, and Sentry is the common alternative.",
    "authorization": "Enforced on the server. A client-side check is a UX affordance; the bundle is on the user's device and can be read.",
    "done_checks": [
      "npx expo-doctor",
      "npx tsc --noEmit",
      "npm test",
      "a build for each shipped platform",
      "install on a real device, not only the simulator"
    ],
    "rules": [
      "Every asynchronous operation a user can see declares four states: loading, empty, error and success. Empty is not success with no rows, and success on a mutation is a confirmation, not a silent return to normal.",
      "Loading affordance matches what you know: a progress bar with a percentage only for determinate work; a skeleton or spinner otherwise. Never a percentage you cannot compute.",
      "Every feature that writes data states its offline behaviour, even if the decision is to fail loudly.",
      "Stored data has a migration path. You cannot ask users to reinstall.",
      "A denied permission is a supported path, not a crash.",
      "Assume several app versions are live at once - a server change that only works with the newest client breaks the rest.",
      "Errors are reported off-device or they are invisible to you.",
      "No secret in the JS bundle. It ships to the device and can be extracted."
    ]
  },
  "dead_code_risks": [
    "Screens and layouts resolved by expo-router filesystem convention, never imported",
    "Config plugins referenced by name in app.json",
    "Native module bridges referenced only from Kotlin/Swift sources",
    "Assets referenced by require() with a computed path",
    "Deep link handlers reachable only from a URL scheme"
  ]
}
