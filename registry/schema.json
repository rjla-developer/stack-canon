{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://raw.githubusercontent.com/rjla-developer/stack-canon/main/registry/schema.json",
  "title": "stack-canon stack registry entry",
  "description": "One file per stack. Describes how to detect the stack, which official agent skills exist for it, what those skills do not cover, and the mentor's stack-specific thresholds.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "stack",
    "display_name",
    "category",
    "detection",
    "last_verified",
    "official_skills",
    "gap_map",
    "testing",
    "growth_thresholds",
    "dead_code_risks"
  ],
  "properties": {
    "stack": {
      "type": "string",
      "pattern": "^[a-z0-9-]+$",
      "description": "Registry key. Must equal the filename without .json."
    },
    "display_name": {
      "type": "string",
      "minLength": 1
    },
    "category": {
      "type": "string",
      "enum": [
        "frontend",
        "backend",
        "fullstack",
        "mobile",
        "infra"
      ]
    },
    "detection": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "files"
      ],
      "properties": {
        "files": {
          "type": "array",
          "minItems": 1,
          "items": {
            "type": "string"
          },
          "description": "Manifest files whose presence suggests this stack. Forward slashes only."
        },
        "markers": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Substrings inside those files that confirm the stack. Needed when one manifest serves many stacks (package.json, pyproject.toml)."
        }
      }
    },
    "last_verified": {
      "type": "string",
      "format": "date",
      "pattern": "^[0-9]{4}-[0-9]{2}-[0-9]{2}$",
      "description": "Date a human last confirmed every id, install command and source_url in this file."
    },
    "official_skills": {
      "type": "array",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": [
          "id",
          "owner",
          "covers",
          "does_not_cover",
          "source_url"
        ],
        "properties": {
          "id": {
            "type": "string",
            "description": "Skill repository in owner/repo form, or owner/repo#skill-name for a single skill inside a collection."
          },
          "owner": {
            "type": "string",
            "minLength": 1
          },
          "owner_kind": {
            "type": "string",
            "enum": [
              "framework-team",
              "vendor",
              "community"
            ],
            "description": "Who publishes it. The mentor states this when recommending, because a community skill carries different risk than a framework-team one."
          },
          "skills": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Individual skill names inside the repository, when it is a collection."
          },
          "install": {
            "type": [
              "string",
              "null"
            ],
            "description": "Verbatim install command. null when unverified; then needs_verification must be true."
          },
          "covers": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "minItems": 1
          },
          "does_not_cover": {
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "source_url": {
            "type": "string",
            "format": "uri"
          },
          "needs_verification": {
            "type": "boolean",
            "description": "true when any field here could not be confirmed against a primary source. The mentor must say so out loud before recommending it."
          },
          "notes": {
            "type": "string"
          }
        }
      }
    },
    "gap_map": {
      "type": "array",
      "description": "What no official skill for this stack covers. Empty array is a claim that nothing is missing - justify it in notes.",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": [
          "gap",
          "why_it_matters",
          "fallback"
        ],
        "properties": {
          "gap": {
            "type": "string"
          },
          "why_it_matters": {
            "type": "string"
          },
          "filled_by": {
            "type": [
              "string",
              "null"
            ]
          },
          "fallback": {
            "type": "string"
          },
          "needs_verification": {
            "type": "boolean"
          }
        }
      }
    },
    "testing": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "layers",
        "commands"
      ],
      "properties": {
        "layers": {
          "type": "array",
          "minItems": 1,
          "description": "Either plain names (legacy) or objects carrying the judgment. Objects are strongly preferred: the name of a layer teaches nothing, but knowing what belongs in it and what it costs is the whole point.",
          "items": {
            "oneOf": [
              {
                "type": "string"
              },
              {
                "type": "object",
                "additionalProperties": false,
                "required": [
                  "name",
                  "belongs_here"
                ],
                "properties": {
                  "name": {
                    "type": "string"
                  },
                  "belongs_here": {
                    "type": "string"
                  },
                  "does_not_belong_here": {
                    "type": "string"
                  },
                  "cost": {
                    "type": "string",
                    "description": "Confidence gained against maintenance and runtime paid."
                  },
                  "signal_you_picked_wrong": {
                    "type": "string",
                    "description": "The observable tell that a test is at the wrong layer - e.g. needing a widget pump to check a pure calculation."
                  }
                }
              }
            ]
          }
        },
        "commands": {
          "type": "object",
          "minProperties": 1,
          "additionalProperties": {
            "type": "string"
          },
          "description": "Keyed by purpose. The mentor runs the full-suite command before reporting any task complete."
        },
        "notes": {
          "type": "string"
        },
        "rules": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Checkable test rules for this stack, written verbatim into the project's CLAUDE.md. Stack-specific only: the generic doctrine is rule 2 of SKILL.md and repeating it wastes the 150-line budget."
        },
        "traps": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Ways tests lie in THIS stack - the suite passes and the product is broken, or the suite fails for a reason unrelated to the change. This is the field a model is least likely to volunteer on its own."
        },
        "what_not_to_test": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Tests that cost maintenance and buy nothing in this stack. Naming these prevents the busywork a coverage target produces."
        },
        "proportions": {
          "type": "string",
          "description": "What the framework team says about the shape of the suite, attributed. The shape is the point; any number is illustration."
        },
        "needs_verification": {
          "type": "boolean"
        }
      }
    },
    "growth_thresholds": {
      "type": "object",
      "minProperties": 1,
      "additionalProperties": {
        "type": "string"
      },
      "description": "Growth thresholds for this stack. Every value states the number AND the unit so the threshold is auditable."
    },
    "dead_code_risks": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "Patterns in this stack that look unreferenced but are not. Read before proposing any deletion: removal needs written evidence of non-use."
    },
    "notes": {
      "type": "string"
    },
    "architecture": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "pattern",
        "folder_strategy",
        "rules",
        "source_url",
        "recommended_by"
      ],
      "description": "The architecture the framework team recommends, the named variants a senior would choose between, and the checkable rules the mentor writes into the project's CLAUDE.md. This is routing and judgment, not documentation: never explain what a pattern IS - record which variant this framework's team recommends, when to deviate, and what breaks if you choose wrong.",
      "properties": {
        "recommended_by": {
          "type": "string",
          "enum": [
            "framework-team",
            "community",
            "mentor"
          ],
          "description": "framework-team means the maintainers published it. Anything else the mentor must present as an opinion, not as doctrine."
        },
        "source_url": {
          "type": "string",
          "format": "uri"
        },
        "pattern": {
          "type": "string",
          "description": "Named pattern, e.g. 'layered MVVM'."
        },
        "layers": {
          "type": "array",
          "items": {
            "type": "object",
            "additionalProperties": false,
            "required": [
              "name",
              "holds"
            ],
            "properties": {
              "name": {
                "type": "string"
              },
              "holds": {
                "type": "string"
              },
              "must_not": {
                "type": "string"
              },
              "optional": {
                "type": "boolean",
                "description": "true when the framework team calls this layer optional. The mentor must not add an optional layer without an observed reason - that is the over-abstraction growth signal."
              }
            }
          }
        },
        "folder_strategy": {
          "type": "string",
          "enum": [
            "feature-first",
            "layer-first",
            "hybrid",
            "unspecified"
          ],
          "description": "The single most consequential structural choice, and the one teams get wrong once and pay for permanently."
        },
        "variants": {
          "type": "array",
          "description": "The real decision a senior makes. Each variant names when it wins and what it costs.",
          "items": {
            "type": "object",
            "additionalProperties": false,
            "required": [
              "name",
              "use_when",
              "cost"
            ],
            "properties": {
              "name": {
                "type": "string"
              },
              "use_when": {
                "type": "string"
              },
              "cost": {
                "type": "string"
              }
            }
          }
        },
        "rules": {
          "type": "array",
          "minItems": 1,
          "items": {
            "type": "string"
          },
          "description": "Checkable invariants, written verbatim into the project's CLAUDE.md. Each must be verifiable by reading the code - 'domain imports no SDK' passes, 'keep it clean' does not."
        },
        "component_library": {
          "type": "string",
          "description": "When and how this stack organises shared UI, and at what point a component library stops being premature."
        },
        "needs_verification": {
          "type": "boolean"
        },
        "notes": {
          "type": "string"
        }
      }
    },
    "key_decisions": {
      "type": "array",
      "description": "The decisions this stack FORCES on every new project - the ones with no framework default, where a silent pick is inherited for the life of the codebase. The mentor surfaces all of them at Step 0/1. This exists because an agent asking the right question is otherwise a matter of luck, and luck does not repeat.",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": [
          "decision",
          "why_it_is_forced",
          "cost_of_getting_it_wrong"
        ],
        "properties": {
          "decision": {
            "type": "string"
          },
          "why_it_is_forced": {
            "type": "string",
            "description": "Why the framework does not decide it for you."
          },
          "options": {
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "reasonable_default": {
            "type": "string"
          },
          "cost_of_getting_it_wrong": {
            "type": "string",
            "description": "What it costs to change later. This is what makes it worth interrupting the user for."
          },
          "needs_verification": {
            "type": "boolean"
          }
        }
      }
    },
    "operability": {
      "type": "object",
      "additionalProperties": false,
      "description": "What this stack needs to survive real users, expressed as things a reader can check. This records what each gate looks like in THIS stack, as something a reader can check.",
      "properties": {
        "failure_boundaries": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "What counts as a call leaving the process in this stack. Naming them is most of the work: an agent cannot state a failure policy for a boundary it did not notice was one."
        },
        "observability": {
          "type": "string",
          "description": "The stack's own idiom for it - the file, hook or interface where instrumentation belongs, so it is not invented from scratch."
        },
        "authorization": {
          "type": "string",
          "description": "Where the authorization decision lives in this stack, and what an unguarded entry point looks like."
        },
        "done_checks": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Commands that prove shippable rather than merely green. A passing suite and a clean build are different claims."
        },
        "rules": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Checkable operability rules for this stack, written into the project's CLAUDE.md when the stage makes them due."
        },
        "needs_verification": {
          "type": "boolean"
        },
        "notes": {
          "type": "string"
        }
      }
    }
  }
}
